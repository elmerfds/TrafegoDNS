#!/command/with-contenv bash
# shellcheck shell=bash

cd /app || exit 1

echo
echo "-------------------------------------"
echo "Starting TrafegoDNS v2..."
echo "-------------------------------------"
echo

# Run as abc user (PUID/PGID and Docker socket permissions handled by init-adduser)
exec s6-setuidgid abc node dist/index.js
